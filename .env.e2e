# E2E-only environment (committed intentionally).
# Values are public placeholders pointing at the same origin; Playwright
# specs intercept `/api-mock/**` and fulfill responses. Never put real
# secrets here.
VITE_SUPABASE_URL=http://localhost:5173/api-mock
VITE_SUPABASE_ANON_KEY=e2e-anon-key
